<form class="{{cssClass}} flexcol" autocomplete="off">
  
  <header class="actions-header">
    <h2>{{actor.name}} - 행동 목록</h2>
    <button type="button" class="item-create" data-type="action" title="행동 추가">
      <i class="fas fa-plus"></i> 행동 추가
    </button>
  </header>

  <section class="actions-body">
    <div class="actions-list">
      {{#if actions.length}}
        {{#each actions as |item id|}}
        <div class="action-item" data-item-id="{{item._id}}">
          <div class="action-header">
            <h3 class="action-name">{{item.name}}</h3>
            <span class="action-type-badge {{item.system.actionType}}">{{lookup ../actionTypes item.system.actionType}}</span>
            <div class="action-controls">
              <a class="item-control item-roll" title="사용"><i class="fas fa-dice-d20"></i></a>
              <a class="item-control item-edit" title="편집"><i class="fas fa-edit"></i></a>
              <a class="item-control item-delete" title="삭제"><i class="fas fa-trash"></i></a>
            </div>
          </div>
          <div class="action-details">
            {{#if item.system.description}}
            <p class="action-description">{{item.system.description}}</p>
            {{/if}}
            {{#if item.system.checkTarget}}
            <div class="action-check">
              <i class="fas fa-dice"></i> 
              <span>판정: {{item.system.checkTarget}}</span>
            </div>
            {{/if}}
          </div>
        </div>
        {{/each}}
      {{else}}
        <div class="empty-state">
          <p>아직 행동이 없습니다.</p>
          <p class="hint">위의 "행동 추가" 버튼을 눌러 새로운 행동을 만드세요.</p>
        </div>
      {{/if}}
    </div>
  </section>

</form>
